# 수업 운영과 성적 산출

지금까지 이 저장소는 상호평가 하나만 다뤘다. 실제로 한 학기를 돌리려면 주차·공지·자료·과제·출석이
같은 곳에 있어야 하고, 학기 말에 그것들이 하나의 성적으로 합쳐져야 한다. 이 문서는 그 부분의 설계다.

참고한 화면은 `rest.dreamitbiz.com/admin` (AI Reboot Academy 관리자)이다. 거기서 가져온 구성은
**주차별 자료·과제 / 출결일지 / 항목별 점수표(강사 합계와 자동 합계를 나란히) / 집계표·등수표**다.

## 무엇이 생겼나

| 테이블 | 하는 일 |
|---|---|
| `course_weeks` · `course_sessions` | 주차와 회차. 출석은 주차가 아니라 **회차**에 붙는다 (주 2회 과목 때문) |
| `notices` | 과목공지 |
| `materials` | 자료실 · 학습자료실 · 강의계획서 (`kind` 로 구분) |
| `tasks` · `task_submissions` | 과제와 제출물. 과제마다 개인/팀을 따로 정한다 |
| `attendance` · `attendance_imports` | 출결과 헤이영 파일 업로드 이력 |
| `exams` · `exam_scores` | 중간 · 기말 |
| `grade_policies` | 과목별 성적 구성비 |
| `final_grades` | 합산 결과 |

`courses` 에 세 칸이 붙었다.

- `peer_assessment` — 상호평가를 하는 과목인가
- `project_mode` — `none` / `individual` / `team`
- `ext_lms_url` — 학교 LMS 주차 목록 주소 (가져오기 원본)

## 누가 무엇을 보는가

확정된 공개 정책이다. **화면에서 감추는 방식이 아니라 RLS 정책을 아예 만들지 않는 방식**으로 막는다.

| 데이터 | 교수 | 학생 |
|---|---|---|
| 주차 · 공지 · 자료 | 전부 | 공개된 것만 |
| 과제 | 전부 | 초안 아닌 것만 |
| **과제 점수 · 피드백** | 전부 | **본인 것만 공개** |
| **출결** | 전부 | **본인 것만 공개** |
| 시험 일정 | 전부 | 볼 수 있음 |
| 시험 점수 (`exam_scores`) | 전부 | **정책 없음 = 못 읽음** |
| 상호평가 원점수 (`results`) | 전부 | **정책 없음 = 못 읽음** |
| 최종 성적 (`final_grades`) | 전부 | **정책 없음 = 못 읽음** |

학생이 제출물을 고칠 수 있는 조건도 정책에 박혀 있다 — 과제가 열려 있고 **아직 채점 전**일 때만이다.
그리고 학생이 `score` / `feedback` / `graded_at` 칸에 값을 넣는 UPDATE 는 `with check` 에서 막힌다.
자기 점수를 스스로 적어 넣는 경로가 없다.

## 성적이 어떻게 나오는가

`compute_final_grades(course_id)` 하나가 유일한 계산 경로다. 클라이언트가 보낸 총점을 저장하는
길은 만들지 않는다.

```
출석 = attendance_pct × min(인정회차 / 전체회차, 1)
        인정회차: 출석 1 · 지각 late_credit(기본 0.5) · 인정결석 excused_credit(기본 1) · 결석 0
과제 = task_pct     × min(획득 / 만점합, 1)     ← 팀 과제는 팀 점수를 팀원 전원에게
중간 = midterm_pct  × min(획득 / 만점합, 1)
기말 = final_pct    × min(획득 / 만점합, 1)
상호 = peer_pct     × min(확정된 results 평균 / 100, 1)

총점 = 다섯 개의 합
```

- 구성비 합은 `grade_policies` 의 CHECK 로 **항상 100** 이 되게 강제한다.
- 결석률이 `absence_limit`(기본 0.25) 을 넘으면 총점과 무관하게 **F**.
- 아직 자료가 없는 항목(시험을 안 봤다든지)은 **만점 처리**한다. 학기 중간에 계산해도
  0점 폭탄이 뜨지 않게 하려는 것이다. 학기 말에는 반드시 자료가 다 들어와야 한다.
- 상호평가를 안 하는 과목(`peer_assessment=false`)은 `peer_pct` 를 0 으로 두고 그 몫을 다른 항목에 나눈다.
- 교수가 `status='approved'` 로 확정한 줄은 **다시 계산해도 덮어쓰지 않는다.**

등급은 95/90/85/80/75/70/65/60 경계로 A+ ~ F.

## 헤이영 출석 가져오기

헤이영에는 공개 API 가 없다. 그래서 **교수가 내려받은 파일을 올리는 방식**이다.

1. 헤이영에서 출결을 엑셀로 내려받는다.
2. 엑셀에서 `다른 이름으로 저장 → CSV UTF-8` 로 바꾼다.
   (브라우저에서 `.xlsx` 를 직접 읽으려면 라이브러리가 필요해서 일부러 CSV 만 받는다.)
3. 과목 → 출석 → 파일 올리기.

읽는 쪽은 `web/src/lib/heyyoung.ts` 다. 네트워크도 DB 도 건드리지 않는 순수 함수라
`npm test` 로 그대로 검증된다 (검사 39개).

파일 형식이 학교마다·버전마다 달라서 이렇게 처리한다.

- 머리글 이름을 못 박지 않고 **별칭 목록**으로 찾는다 (`학번`/`학생번호`/`사번`, `상태`/`출결`/`구분` …).
- 헤이영이 위에 제목·기간 줄을 얹어 주므로 **학번처럼 보이는 열이 있는 첫 줄**을 머리글로 잡는다.
- 구분자(`,` `\t` `;`)를 첫 줄에서 자동으로 고른다. BOM·CRLF·따옴표 안의 쉼표를 처리한다.
- `인정결석` 을 `결석` 보다 **먼저** 판정한다. 순서를 바꾸면 공결이 결석이 된다.
- **알아볼 수 없는 줄은 버리지 않는다.** `skipped` 에 줄 번호와 이유를 담아 돌려주고 화면에 보여 준다.
  조용히 사라지면 출석이 빠진 채로 성적이 나간다.

명단 대조(`matchRoster`)는 **학번만** 열쇠로 쓴다. 이름은 동명이인·개명 때문에 쓰지 않는다.
결과는 셋으로 나뉜다 — 맞은 행, 명단에 없는 학번, 파일에 한 번도 안 나온 학생(결석 처리 후보).

## 학교 LMS(dima) 연동

방향은 **학교 LMS 가 원본, 이쪽이 사본**이다. 주차·공지·자료·과제를 학교 LMS 에서 읽어 와 복제한다.

그래서 `course_weeks` / `notices` / `materials` / `tasks` 에 두 칸이 있다.

- `ext_ref` — 원본 쪽 식별자
- `locked` — 여기서 사람이 고친 줄. 가져오기가 덮어쓰지 않는다

`bridge/` 의 기존 원칙은 그대로다. **읽기는 HTTP, 쓰기는 브라우저 자동화 + 사람 확인**이고,
로그인 캡차는 우회하지 않는다 (로그인 화면에 `slidercaptcha` 가 있다).

### 2026-08-10 에 로그인 상태에서 확인한 구조

교수 계정으로 들어가 **읽기만** 하고 확인했다. 추측이 아니다.

| 화면 | 주소 |
|---|---|
| 내 강의 목록 | `/lms/myLecture/doListView.dunet` |
| 강의실 홈 | `/lms/class/classroom/doViewClassRoom.dunet` |
| **주/회차 관리** | `/lms/class/course/manage/doListCourseContents.dunet` |

강의실 좌측 메뉴: 강의홈 · **주/회차 관리** · 과목공지 · 학습자료실 · 질문답변 · 자유게시판 ·
**과제관리** · 토론관리 · **팀프로젝트출결/채점** · 퀴즈관리 · 설문등록/결과조회 ·
학습현황/통계 · **성적산출/결과** · 청강생등록 · 조교관리 · 자료 저장 · 강의메뉴관리 ·
학생모드 환경보기 · **강의계획서**

주/회차 화면의 마크업:

```
.learning_plan_set                       주차 전체를 감싸는 상자
  .module_title_row.sortable_seq         주차 머리 — "1 1주차 (수강기간: 26.03.09 ~ 26.03.17)"
    .title > strong                      "1주차"
  .module_detail_row.sortable_seq        회차 하나
    .top > strong.title                  "1회차 : 1주 1"
    .top 안의 나머지 글자                "(출석여부 : 출석반영) 학생공개 | 학습시간: 100 분"
    .info .learnitem_element > a         학습요소 — 배지 글자가 "자료실" 또는 "과제"
                                         본문에 출석인정시간 / 출석인정기간 / 제출인정기간
```

강의 목록 화면의 자바스크립트 호출로 식별자를 얻는다.

- `fncGoClassroom(extCourseId, classNo, '3')` — 강의실 입장
- `fnGoContent(type, extCourseId, classNo, contentId, 'F')` — `type=9` 자료실, `type=3` 과제

`extCourseId` 는 `202610UN006071667672Y5` 꼴이다. 앞 6자리가 학기(`202610`), 끝 두 자가 분반이다.
이 값을 `courses.ext_course_id` 에 넣어 두었다 (`supabase/seed/2026-1-courses.sql`).

> **주의** — 학교 LMS 는 회차마다 `출석여부: 출석반영` 이 붙어 있다. 그쪽 출석은
> 온라인 학습요소 이수로 매겨지는 것이고, 헤이영 출석(대면)과는 별개다.
> 둘을 같은 칸에 섞지 말 것.

셀렉터는 확인했지만 가져오기 코드는 아직 붙이지 않았다. `SELECTORS_VERIFIED` 를 켜는 것은
파서를 쓰고 실제 페이지로 검증한 뒤다.

## 남은 작업

- [x] 교수 화면 — 주차, 공지·자료, 과제 등록·채점, 출석 가져오기·수정, 성적 산출·확정
- [ ] 학생 화면 — 주차별 자료·과제 보기, 과제 제출, 본인 출결·과제 점수·감점 확인
- [x] 표 내보내기 — CSV · XLSX(무압축 ZIP 직접 생성) · PDF(인쇄 창). 출결일지 · 감점현황 · 성적표
- [ ] 학교 LMS 주차 가져오기 — 셀렉터는 확인했다. 파서만 쓰면 된다
- [x] 6과목 시드 (`supabase/seed/2026-1-courses.sql`) — 과목명·분반·`ext_course_id` 를
      학교 LMS 에서 직접 확인해 채웠다

## 감점 요소와 기타 성적

지각·태도불량·과제미제출·과제 지각제출을 항목으로 만들고 건수를 세서 기타 성적을 낸다.

```
기타 = etc_pct × max(etc_base − 감점합, 0) / etc_base
```

**기록하는 자리에서 자동으로 센다.** 따로 옮겨 적지 않는다.

| 항목 | 기본 점수 | 언제 기록되나 |
|---|---|---|
| 지각 | 1 | 출석을 정리할 때. `attendance.status='late'` 를 센다 |
| 조퇴 | 1 | 출석을 정리할 때. `status='early_leave'` |
| 태도 불량 | 2 | 출석 탭 출결일지 오른쪽 칸에 건수를 넣는다 |
| 과제 미제출 | 5 | 과제 제출물이 없거나 `submitted_at` 이 비어 있으면 |
| 과제 지각 제출 | 2 | `submitted_at > due_at` 이면 |

세는 일은 `deduction_summary()` 하나가 전담한다. 화면에서 합치지 않는다.
점수와 항목 이름은 과목마다 **감점** 탭에서 바꾼다. 감점은 0 아래로 내려가지 않는다.

학생은 감점 **항목 이름**은 보고(왜 깎였는지 알아야 한다), 자기 감점 기록만 본다.

## 표 내보내기

`web/src/lib/exporters.ts` 하나가 CSV · XLSX · PDF 를 모두 맡는다. 라이브러리는 넣지 않았다.

- **CSV** — BOM 을 붙여 엑셀에서 한글이 깨지지 않게 한다
- **XLSX** — 무압축(stored) ZIP 을 CRC32 부터 직접 만든다. 진짜 `.xlsx` 라 엑셀이 그대로 연다.
  실제 Excel 로 열어 값을 되읽어 확인했다 (시트 2개 · 한글 시트명 · 숫자는 숫자형 · 쉼표 든 이름 보존)
- **PDF** — 만들지 않는다. 인쇄 창을 띄우고 사용자가 "PDF로 저장"을 고른다.
  한글 PDF 를 직접 만들려면 폰트를 통째로 심어야 해서 수 MB 가 붙고 글자도 잘 깨진다

내보내기 버튼이 붙은 곳: 출석 탭(회차별 출결일지) · 감점 탭(감점 현황) · 성적 탭(성적표).

## 헤이영 출석부는 가로형이다

세로형(`학번, 날짜, 상태`)과 가로형(학생 × 주차·교시) 둘 다 읽는다.
실제 내려받기는 **가로형**이고 학기 전체가 한 파일에 들어 있다.

| 기호 | 뜻 |
|---|---|
| `O` | 출석 |
| `◎` | **유고결석** — 출석이 아니다 |
| `△` | 지각 |
| `X` | 결석 |
| `□` | 조퇴 |
| `-` | 미정 — 넣지 않는다 |

헤이영의 "N주차 M번째" 가 `course_weeks.week_no` + `course_sessions.session_no` 와 그대로 대응한다.
그래서 **회차를 먼저 만들어 두어야** 붙는다. 회차가 없는 칸은 넣지 않고 화면에 목록으로 알려 준다.
