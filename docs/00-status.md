# 지금 어디까지 왔나 — 2026-08-29 점검

이 문서는 **측정한 값**만 적는다. 다른 문서의 "적용 완료" 를 옮겨 적지 않았다.
(그렇게 해서 한 번 틀린 적이 있다 — `CLAUDE.md` 의 "문서보다 DB 를 믿을 것".)

각 줄 끝에 어떻게 확인했는지를 남겼으므로 **같은 명령을 다시 돌리면 이 표를
갱신할 수 있다.** 날짜가 지난 이 문서를 믿지 말고 다시 재라.

| 영역 | 지금 | 확인한 방법 |
|---|---|---|
| 코드 (`main`) | PR #4~#14 병합 완료 | `git log` |
| 검사 | `weekly` 포함 시험 10종 중 **9종 통과 · `roster-files` 3건 실패**(아래 0절) | `cd web && npm test` |
| 배포 | Actions success · 배포된 화면에서 `주차별 명단` 동작 확인 | `gh run watch` · 화면 |
| DB 마이그레이션 | **`0001`~`0014` 전부 적용** (변동 없음) | 아래 2절 |
| 명단 | 6과목 — **학교 LMS 와 어긋나 있다**. 아래 0절 | 화면 `명단 대조` |
| 연락처(전화번호) | 6과목 **187명 입력됨** | 아래 0절 |
| 1주차 과제 | 3과목 채점 완료 · 3과목 마감 전 | 아래 0절 |
| 주소 (HTTP) | `http://lms.miraejob.co.kr` 뜬다 | `curl` |
| 주소 (HTTPS) | **안 뜬다** — 아래 5절 | `openssl s_client` |

---

## 0. 2026-08-29 에 한 것 — 1주차 운영

### 채점 — 3과목 끝냈다

마감이 **08-28** 인 세 과목이다. 08-30 마감인 `취업과경력개발(Y3)` ·
`문화예술콘텐츠창업(Y5 · Y6)` 은 아직 진행 중이라 손대지 않았다.

| 과목 | 학교 LMS 제출 | 대시보드 채점(1점) | 미제출 표시 |
|---|---|---|---|
| 1인예술과창업실무 Y2 | 15/30 | **15** | 15 |
| 자원관리능력 Y1 | 20/35 | **20** | 15 |
| 창업과기업가정신 Y4 | 17/35 | **17** | 18 |

각 과목에 `1주차 과제`(개인 · **배점 1** · 마감 `2026-08-28 23:59` · 1주차)를 만들고,
학교 LMS `과제관리 → 채점하기` 목록을 `과제 제출현황 ②` 로 반영한 뒤 점수를 넣었다.
과제 상태는 셋 다 **`마감`** 이다 — `준비중` 이면 `학생별 현황` 집계에서 통째로 빠진다.

**채점 규칙** — 마감일까지 1점, 다음날 이후 0.5점. 1주차는 제출기간이
`08-28 23:59` 에 닫혀 지각 제출이 생길 수 없었으므로 **전원 1점**이다.

**학교 LMS 에는 쓰지 않았다.** 읽기만 했다.

### 전화번호 — 처음으로 채웠다

그동안 0건이었다. 학교 LMS `학생별 과제` 의 핸드폰 칸은 **전원 공란**이고
대시보드 `연락처 관리` 도 비어 있었다. 유일한 출처는 헤이영이다 —
`UMS시스템 → 푸시 → 푸시 전송 → 수신자 추가` 에서 강좌를 고르고, 화면 맨 위
**`개인정보마스킹여부` 를 끄고** 표를 통째로 긁는다.

| 과목 | 반영 | 못 넣은 것 |
|---|---|---|
| 1인예술과창업실무 Y2 | 30 | — |
| 자원관리능력 Y1 | 34 | 김수민 — 헤이영에만 있다 |
| 창업과기업가정신 Y4 | 35 | — |
| 취업과경력개발 Y3 | 30 | 5명 |
| 문화예술콘텐츠창업 Y5 | 29 | — |
| 문화예술콘텐츠창업 Y6 | 29 | 1명 |

### 헤이영과 학교 LMS 의 명단이 서로 다르다

같은 과목인데 양쪽 수강생이 어긋난다. 수강정정 시점 차이로 보인다.

- **학교 LMS 에만** — 전은경(Y1) · 김나현(Y2) · 이정현(Y4).
  과제 미제출자인데 헤이영에 없어서 **번호를 못 받았다.**
- **헤이영에만** — 김수민(Y1) · 조혜민(Y2) · 양성일(Y2).

이 여섯 명은 실제 수강 여부를 확인해야 한다.

### 아직 안 한 것 — 명단 제외

대시보드 명단에 지난 학기 학생이 남아 있다. 학교 LMS 에만 있던 학생 11명
(Y4 5 · Y2 4 · Y1 2)은 `명단 대조` 로 **넣었지만**, 반대쪽(이 사이트에만 있는
학생) **제외는 못 눌렀다.**

| 과목 | 지금 | 제외 대상 | 정리 후 |
|---|---|---|---|
| 1인예술과창업실무 Y2 | 64 | 34 | 30 |
| 자원관리능력 Y1 | 101 | 66 | 35 |
| 창업과기업가정신 Y4 | 74 | 39 | 35 |

그래서 `주차별 명단` 의 1주차 미제출이 Y2 기준 **49명**으로 나온다
(실제 15 + 지난 학기 34). 제외를 누르면 맞는다.

`명단 대조` 의 제외는 `confirm()` 대화상자를 쓴다. **삭제가 아니라
`active=false`** 라 기록이 남고 `수강생 관리` 에서 `복귀` 로 되돌릴 수 있다.

### `roster-files.check.ts` 는 이전부터 실패한다

`data/roster/*_202620.tsv` 3개 파일이 머리글 줄 · 학년 칸 때문에 실패한다.
`main` 에서도 같게 실패하는 것을 확인했다. 이번 작업과 무관하며 아직 안 고쳤다.
`npm test` 는 `&&` 로 이어져 있어서 여기서 멈추므로, 나머지 시험은 하나씩 돌려야 한다.

### 남은 손

1. **명단 제외** — 위 표. 과목당 버튼 1번 + `확인` 1번
2. **학과 · 학년** — 대시보드 명단에 비어 있어 `주차별 명단` 에 `—` 로 나온다
3. **Y3 · Y5 · Y6** — 08-30 마감. 그 뒤에 같은 절차를 한 번 더
4. **HTTPS** — 아래 5절. 그대로 남아 있다

## 1. 2026-08-23 에 들어간 것

| PR | 무엇 |
|---|---|
| #9 | 연락처 관리 · 일자별 기록 · 문자 · 알림 (`0012` · `0013`) |
| #7 | 명단 교체 — 붙여넣은 표에 없는 학생을 지운다 |
| #8 | 출석부를 올리면 없는 회차를 만들어 가며 적재한다 |
| #10 | 헤이영 **푸시** 로 보내는 자리 (`0014`) |
| #11 | 연락처 붙여넣기가 학과를 이름으로 집던 버그 |
| #6 | 화면에서 이모지 제거 |

## 2. DB — 무엇이 실제로 올라가 있나

`0011` · `0012` · `0013` · `0014` 를 이날 SQL Editor 에서 올렸다.
REST 로 다시 물어 확인한 결과가 아래다. 전부 200 이다.

| 확인한 것 | 결과 |
|---|---|
| `students.grade` · `dept` (`0011`) | 200 |
| `student_contacts` · `session_marks` · `mark_policies` (`0012`) | 200 |
| `message_templates` · `message_log` (`0013`) | 200 |
| `message_log.channel` 에 `heyyoung_push` (`0014`) | 제약에 포함됨 |

```bash
cd web && U=$(grep VITE_SUPABASE_URL .env | cut -d= -f2-) \
        && K=$(grep VITE_SUPABASE_ANON_KEY .env | cut -d= -f2-)
for t in student_contacts session_marks mark_policies message_templates message_log; do
  echo -n "$t "; curl -s -o /dev/null -w '%{http_code}\n' "$U/rest/v1/$t?select=*&limit=1" -H "apikey: $K"
done
```

`404` 는 표가 없는 것이고 `200`(빈 배열 포함)이면 표는 있고 anon 이 못 읽는
것뿐이다. **둘을 헷갈리지 말 것** — RLS 로 막힌 것과 표가 없는 것은 다르다.

> **SQL Editor 에는 확인 대화상자가 있다.** `Run` 을 눌러도
> "Potential issue detected — Run query / Cancel" 이 뜨면 거기서 한 번 더
> 눌러야 실제로 돈다. 이걸 못 보고 앞 실행의 "Success" 를 그대로 읽으면
> **안 돌아간 것을 돌아갔다고 착각한다.** 실제로 그렇게 한 번 틀렸다.

## 3. 명단 — 학기가 바뀌며 두 벌이 쌓였던 것

2026-08-12 에 넣은 199명은 **지난 학기 명단**이었다. 이번 학기 명단과
학번이 하나도 겹치지 않아서, 그냥 붙여넣자 한 과목 안에 두 학기가 쌓였다.

6과목 모두 이번 학기 명단을 다시 넣었다. `0011` 이 올라간 뒤라 이번에는
**학년 · 학과까지 저장됐다.**

| 과목 | 분반 | 이번 학기 |
|---|---|---|
| 자원관리능력 | Y1 | 35 |
| 1인예술과창업실무 | Y2 | 30 |
| 취업과경력개발 | Y3 | 35 |
| 창업과기업가정신 | Y4 | 35 |
| 문화예술콘텐츠창업 | Y5 | 30 |
| 문화예술콘텐츠창업 | Y6 | 30 |
| **합계** | | **195** |

### 아직 남은 것 — 옛 명단 199명

지우기 전 검증은 끝났다. **옛 명단 정확히 199명, 딸린 평가 · 출결 · 과제 ·
성적 0건.** 지워도 함께 사라질 것이 없다.

`supabase/run-roster-replace.sql` 의 ② 블록을 SQL Editor 에서 돌리면 된다.
`created_at` 으로 두 학기를 가른다 — 8/12 에 들어간 199명과 8/23 에 들어간
195명이다. `begin/commit` 으로 감쌌으니 숫자가 이상하면 `rollback` 한다.

## 4. 출결 — 회차 30개, 표시는 0칸

6과목 모두 **15주 × 2교시 = 30회차**를 만들고 헤이영 출석부를 적재했다.
적재된 출결은 **0칸**이다 — 파일이 아직 빈 출석부이기 때문이다.
첫 수업이 08/24(월) · 08/26(수)이니 정상이다.

출석을 부른 뒤 헤이영에서 다시 내려받아 같은 자리에 올리면 채워진다.
그리고 **PR #8 이후로는 회차를 미리 만들 필요가 없다** — 출석부 파일에
주차 · 교시 · 날짜가 다 들어 있어서, 없는 회차를 만들어 가며 적재한다.

수업일(`meets_on`)은 아직 비어 있다. 다음 적재 때 파일 날짜로 채워진다.

## 5. 주소 — HTTP 는 뜨고 HTTPS 는 안 뜬다

DNS 와 Pages 설정은 끝나 있다. 막힌 것은 인증서 하나다.

```
$ openssl s_client -servername lms.miraejob.co.kr -connect lms.miraejob.co.kr:443
subject=CN=*.github.io          ← 우리 도메인 이름이 들어 있지 않다
```

GitHub 이 `lms.miraejob.co.kr` 용 인증서를 아직 발급하지 않았다.

**이건 불편함으로 끝나지 않는다.** 2026-08-23 에 실제로 사고가 났다 —
평문 HTTP 요청을 **공유기가 가로채** 설정 페이지(`192.168.8.1`)로 돌려 버려서
화면이 통째로 안 열렸다. 학교 밖 · 공용 와이파이에서 학생이 겪을 일이다.

**할 일** — 저장소 `Settings → Pages` 에서 Custom domain 을 **지웠다가 다시
저장**한다. 초록불 뒤 인증서가 발급되면 **`Enforce HTTPS` 를 켠다.**
인증서 없이 켜면 사이트가 통째로 안 열린다. 발급 여부는 위 `openssl` 한 줄로 본다.

## 6. 헤이영 — 무엇이 되고 무엇이 안 되나

교수 계정으로 직접 열어 보고 적었다. 자세한 것은
[`70-attendance-contacts.md`](70-attendance-contacts.md).

- **문자발송 화면이 교수 메뉴에 없다.** `UMS시스템` 아래는 푸시뿐이다.
  코드에 있던 `/admin/screen/HCO0301M01` 은 404 였고, 그마저 출석부 화면 코드였다.
- **푸시는 된다** — `푸시 전송 · 컨텐츠관리 · 전송결과 · 전송통계`.
  다만 **헤이영캠퍼스 앱을 깐 학생만** 받는다. 누가 깔았는지는 알 길이 없다.
- 번호를 걷는 가장 빠른 길은 **푸시 `수신자 추가`** 다. 강좌를 고르면 그 강좌
  학생의 이름 · 학번 · 학과 · 학년 · 전화번호가 한 표에 나온다.
  맨 위 `개인정보마스킹여부` 를 끄고 통째로 긁어 `연락처 관리` 에 붙인다.
- 여전히 **이쪽이 대신 쏘지 않는다.** 공개 API 가 없고 전송 엔드포인트는 추측이다.
  제목 · 내용 · 학번 목록까지만 만들고 `메시지전송` 은 사람이 누른다.

## 7. 사람 손이 남은 것

1. **옛 명단 199명 삭제** — 3절
2. **HTTPS** — 5절
3. **과목마다 `입장 승인 → 명단 전원 승인`** — 아직 안 눌렀다.
   누르기 전에는 학생이 로그인해도 승인 대기 줄에만 선다. 의도한 동작이다.
4. **연락처 채우기** — 6절의 빠른 길로 6과목을 한 번씩
5. **학교 LMS 성적 입력** — 화면 셀렉터 미확인으로 잠겨 있다
   (`SELECTORS_VERIFIED = false`). 그때까지는 CSV 를 내려받아 손으로 넣는다.

## 8. 저장소 정리

강의자료는 **[`jungaistar/lecture-materials`](https://github.com/jungaistar/lecture-materials)**
로 내보냈다 (2026-08-23). `농협/` 을 `jungaistar/nonghyup` 으로 뺀 것과 같은 판단이다 —
성적을 다루는 운영 시스템과 강의 자료는 고치는 이유도 배포 주기도 다르다.

남은 가지 둘은 이제 필요 없다.

| 가지 | 왜 |
|---|---|
| `claude/github-connection-yk085m` | #9 의 먼저 판. 표 이름이 바뀌며 다시 쓰였다 |
| `claude/learning-materials-…-a57o7t` | 강의자료의 먼저 판. 나중 판을 새 저장소로 옮겼다 |

학기 운영 순서는 [`30-operations.md`](30-operations.md), 날짜순 이력은
[`90-worklog.md`](90-worklog.md) 에 있다.
